<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="CipherKey">
<meta name="theme-color" content="#0d0d1a">
<title>CipherKey</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=IBM+Plex+Sans:wght@300;400;500&display=swap');

  :root {
    --bg: #0d0d1a;
    --surface: #13131f;
    --surface2: #1a1a2e;
    --surface3: #21213a;
    --accent: #7f77dd;
    --accent-dim: #534ab7;
    --accent-glow: rgba(127,119,221,0.15);
    --text: #f0eeff;
    --text-dim: #9892c8;
    --text-muted: #4e4a7a;
    --border: rgba(127,119,221,0.18);
    --border-bright: rgba(127,119,221,0.4);
    --green: #5dcaa5;
    --green-dim: rgba(93,202,165,0.12);
    --red: #f09595;
    --mono: 'IBM Plex Mono', monospace;
    --sans: 'IBM Plex Sans', sans-serif;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }

  html, body {
    height: 100%;
    background: var(--bg);
    color: var(--text);
    font-family: var(--sans);
    overflow: hidden;
    -webkit-font-smoothing: antialiased;
  }

  .app {
    display: flex;
    flex-direction: column;
    height: 100vh;
    height: 100dvh;
    max-width: 430px;
    margin: 0 auto;
    position: relative;
    overflow: hidden;
  }

  /* Subtle grid background */
  .app::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(127,119,221,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(127,119,221,0.04) 1px, transparent 1px);
    background-size: 32px 32px;
    pointer-events: none;
    z-index: 0;
  }

  /* Header */
  .header {
    position: relative;
    z-index: 10;
    padding: env(safe-area-inset-top, 48px) 20px 0;
    background: var(--bg);
  }

  .header-inner {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 0 16px;
  }

  .logo {
    width: 38px;
    height: 38px;
    border-radius: 10px;
    background: var(--surface2);
    border: 1px solid var(--border-bright);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .logo svg {
    width: 20px;
    height: 20px;
    stroke: var(--accent);
    fill: none;
    stroke-width: 1.5;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .header-text h1 {
    font-family: var(--mono);
    font-size: 17px;
    font-weight: 600;
    color: var(--text);
    letter-spacing: 0.04em;
  }

  .header-text p {
    font-size: 11px;
    color: var(--text-muted);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-top: 1px;
  }

  /* Tab bar */
  .tabs {
    display: flex;
    margin: 0 20px 16px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 3px;
    position: relative;
    z-index: 10;
  }

  .tab {
    flex: 1;
    text-align: center;
    padding: 9px 8px;
    border-radius: 8px;
    font-family: var(--mono);
    font-size: 13px;
    font-weight: 500;
    cursor: pointer;
    color: var(--text-muted);
    letter-spacing: 0.05em;
    transition: color .2s, background .2s;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    user-select: none;
  }

  .tab svg {
    width: 14px;
    height: 14px;
    stroke: currentColor;
    fill: none;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
    flex-shrink: 0;
  }

  .tab.active {
    background: var(--surface2);
    color: var(--accent);
    border: 1px solid var(--border-bright);
  }

  /* Scroll area */
  .scroll {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    -webkit-overflow-scrolling: touch;
    position: relative;
    z-index: 5;
    padding-bottom: calc(20px + env(safe-area-inset-bottom, 0px));
  }

  .scroll::-webkit-scrollbar { display: none; }

  /* Panel */
  .panel { display: none; padding: 0 20px; }
  .panel.active { display: block; }

  /* Field */
  .field { margin-bottom: 16px; }

  .field-label {
    font-family: var(--mono);
    font-size: 10px;
    font-weight: 500;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 6px;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .field-label::before {
    content: '';
    display: inline-block;
    width: 3px;
    height: 3px;
    border-radius: 50%;
    background: var(--accent);
  }

  .field-input {
    width: 100%;
    padding: 13px 16px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    font-family: var(--mono);
    font-size: 16px;
    color: var(--text);
    outline: none;
    transition: border-color .2s, box-shadow .2s;
    -webkit-appearance: none;
    appearance: none;
  }

  .field-input::placeholder {
    color: var(--text-muted);
    font-size: 14px;
  }

  .field-input:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px var(--accent-glow);
  }

  /* Grid */
  .grid-wrap {
    margin-bottom: 16px;
  }

  .grid-table {
    width: 100%;
    border-collapse: collapse;
    border-radius: 10px;
    overflow: hidden;
    border: 1px solid var(--border);
    background: var(--surface);
  }

  .grid-table td {
    text-align: center;
    font-family: var(--mono);
    font-size: 14px;
    font-weight: 500;
    width: 16.66%;
    padding: 10px 0;
    border: 1px solid rgba(127,119,221,0.1);
    color: var(--text-dim);
    transition: background .2s, color .2s;
  }

  .grid-table td.kw {
    background: var(--accent-glow);
    color: var(--accent);
    font-weight: 600;
  }

  /* Results */
  .result-block {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 14px 16px;
    margin-bottom: 16px;
  }

  .result-block-label {
    font-family: var(--mono);
    font-size: 10px;
    font-weight: 500;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .result-block-label::before {
    content: '';
    display: inline-block;
    width: 3px;
    height: 3px;
    border-radius: 50%;
    background: var(--green);
  }

  .digrams-wrap {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    min-height: 28px;
  }

  .digram-pill {
    background: var(--surface3);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 4px 10px;
    font-family: var(--mono);
    font-size: 13px;
    font-weight: 500;
    color: var(--text-dim);
    letter-spacing: 0.08em;
    animation: popIn .15s ease;
  }

  @keyframes popIn {
    from { transform: scale(.85); opacity: 0; }
    to { transform: scale(1); opacity: 1; }
  }

  .result-text {
    font-family: var(--mono);
    font-size: 18px;
    font-weight: 600;
    color: var(--green);
    word-break: break-all;
    letter-spacing: 0.06em;
    min-height: 26px;
    line-height: 1.5;
  }

  .result-text.empty {
    color: var(--text-muted);
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0;
  }

  .copy-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 12px;
  }

  .char-count {
    font-family: var(--mono);
    font-size: 11px;
    color: var(--text-muted);
  }

  .copy-btn {
    display: flex;
    align-items: center;
    gap: 5px;
    padding: 7px 14px;
    background: var(--surface2);
    border: 1px solid var(--border-bright);
    border-radius: 8px;
    font-family: var(--mono);
    font-size: 12px;
    font-weight: 500;
    color: var(--accent);
    cursor: pointer;
    letter-spacing: 0.04em;
    transition: background .15s, transform .1s;
    -webkit-appearance: none;
  }

  .copy-btn:active { transform: scale(.96); background: var(--surface3); }

  .copy-btn svg {
    width: 13px;
    height: 13px;
    stroke: currentColor;
    fill: none;
    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .copy-btn.copied {
    color: var(--green);
    border-color: rgba(93,202,165,0.4);
  }

  /* Rules */
  .rules {
    background: var(--surface);
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent-dim);
    border-radius: 10px;
    padding: 12px 14px;
    margin-bottom: 24px;
  }

  .rules p {
    font-size: 12px;
    color: var(--text-muted);
    line-height: 1.7;
    font-family: var(--sans);
  }

  .rules p + p { margin-top: 6px; }

  .placeholder-text {
    font-size: 13px;
    color: var(--text-muted);
    font-family: var(--sans);
  }
</style>
</head>
<body>
<div class="app">

  <div class="header">
    <div class="header-inner">
      <div class="logo">
        <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
      </div>
      <div class="header-text">
        <h1>CipherKey</h1>
        <p>Playfair–Wheatstone · 6×6</p>
      </div>
    </div>

    <div class="tabs" role="tablist">
      <div class="tab active" role="tab" aria-selected="true" onclick="switchTab('encrypt')">
        <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        Encrypt
      </div>
      <div class="tab" role="tab" aria-selected="false" onclick="switchTab('decrypt')">
        <svg viewBox="0 0 24 24"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 5-5"/><path d="M17 7v4"/></svg>
        Decrypt
      </div>
    </div>
  </div>

  <div class="scroll">

    <!-- ENCRYPT PANEL -->
    <div class="panel active" id="panel-encrypt">

      <div class="field">
        <div class="field-label">Keyword</div>
        <input class="field-input" type="text" id="enc-keyword"
          placeholder="e.g. SECRETKEY"
          maxlength="36"
          autocomplete="off" autocorrect="off" autocapitalize="characters" spellcheck="false"
          oninput="this.value=this.value.toUpperCase();updateEncrypt()">
      </div>

      <div class="field">
        <div class="field-label">Plaintext</div>
        <input class="field-input" type="text" id="enc-text"
          placeholder="Text or password to encode"
          autocomplete="off" autocorrect="off" autocapitalize="characters" spellcheck="false"
          oninput="this.value=this.value.toUpperCase();updateEncrypt()">
      </div>

      <div class="grid-wrap">
        <div class="field-label">6×6 Keyword Grid</div>
        <table class="grid-table" id="enc-grid" aria-label="Encryption cipher grid"></table>
      </div>

      <div class="result-block">
        <div class="result-block-label" style="--dot:var(--text-muted)">Digrams</div>
        <div class="digrams-wrap" id="enc-digrams">
          <span class="placeholder-text">Enter text to see digrams…</span>
        </div>
      </div>

      <div class="result-block">
        <div class="result-block-label">Encoded text</div>
        <div class="result-text empty" id="enc-result">Enter plaintext to encode</div>
        <div class="copy-row">
          <span class="char-count" id="enc-count"></span>
          <button class="copy-btn" id="enc-copy-btn" onclick="copyText('enc-result','enc-copy-btn')">
            <svg viewBox="0 0 24 24"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
            Copy
          </button>
        </div>
      </div>

      <div class="rules">
        <p>A–Z and 0–9 only · spaces &amp; punctuation removed · repeated letters in a pair get X inserted · odd final character padded with X</p>
      </div>

    </div><!-- /encrypt -->

    <!-- DECRYPT PANEL -->
    <div class="panel" id="panel-decrypt">

      <div class="field">
        <div class="field-label">Keyword</div>
        <input class="field-input" type="text" id="dec-keyword"
          placeholder="e.g. SECRETKEY"
          maxlength="36"
          autocomplete="off" autocorrect="off" autocapitalize="characters" spellcheck="false"
          oninput="this.value=this.value.toUpperCase();updateDecrypt()">
      </div>

      <div class="field">
        <div class="field-label">Ciphertext</div>
        <input class="field-input" type="text" id="dec-text"
          placeholder="Encoded text to decode"
          autocomplete="off" autocorrect="off" autocapitalize="characters" spellcheck="false"
          oninput="this.value=this.value.toUpperCase();updateDecrypt()">
      </div>

      <div class="grid-wrap">
        <div class="field-label">6×6 Keyword Grid</div>
        <table class="grid-table" id="dec-grid" aria-label="Decryption cipher grid"></table>
      </div>

      <div class="result-block">
        <div class="result-block-label" style="--dot:var(--text-muted)">Ciphertext digrams</div>
        <div class="digrams-wrap" id="dec-digrams">
          <span class="placeholder-text">Enter ciphertext to see digrams…</span>
        </div>
      </div>

      <div class="result-block">
        <div class="result-block-label">Decoded text</div>
        <div class="result-text empty" id="dec-result">Enter ciphertext to decode</div>
        <div class="copy-row">
          <span class="char-count" id="dec-count"></span>
          <button class="copy-btn" id="dec-copy-btn" onclick="copyText('dec-result','dec-copy-btn')">
            <svg viewBox="0 0 24 24"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
            Copy
          </button>
        </div>
      </div>

      <div class="rules">
        <p>Decoded output preserves any filler X values added during encryption for repeated letters or odd-length text.</p>
        <p>A–Z and 0–9 only · ciphertext read in sequential digrams.</p>
      </div>

    </div><!-- /decrypt -->

  </div><!-- /scroll -->
</div><!-- /app -->

<script>
const CHARSET = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';

function buildGrid(keyword) {
  const kw = (keyword || '').toUpperCase().replace(/[^A-Z0-9]/g, '');
  const seen = new Set();
  const grid = [];
  for (const c of kw) { if (!seen.has(c)) { seen.add(c); grid.push(c); } }
  for (const c of CHARSET) { if (!seen.has(c)) { seen.add(c); grid.push(c); } }
  return grid;
}

function renderGrid(tableId, grid, keyword) {
  const kw = new Set((keyword || '').toUpperCase().replace(/[^A-Z0-9]/g, ''));
  const table = document.getElementById(tableId);
  let html = '';
  for (let r = 0; r < 6; r++) {
    html += '<tr>';
    for (let c = 0; c < 6; c++) {
      const ch = grid[r * 6 + c];
      html += `<td class="${kw.has(ch) ? 'kw' : ''}">${ch}</td>`;
    }
    html += '</tr>';
  }
  table.innerHTML = html;
}

function getPos(grid, ch) {
  const i = grid.indexOf(ch);
  return [Math.floor(i / 6), i % 6];
}

function encryptDigram(grid, a, b) {
  const [r1, c1] = getPos(grid, a);
  const [r2, c2] = getPos(grid, b);
  if (r1 === r2) return grid[r1 * 6 + (c1 + 1) % 6] + grid[r2 * 6 + (c2 + 1) % 6];
  if (c1 === c2) return grid[((r1 + 1) % 6) * 6 + c1] + grid[((r2 + 1) % 6) * 6 + c2];
  return grid[r1 * 6 + c2] + grid[r2 * 6 + c1];
}

function decryptDigram(grid, a, b) {
  const [r1, c1] = getPos(grid, a);
  const [r2, c2] = getPos(grid, b);
  if (r1 === r2) return grid[r1 * 6 + (c1 + 5) % 6] + grid[r2 * 6 + (c2 + 5) % 6];
  if (c1 === c2) return grid[((r1 + 5) % 6) * 6 + c1] + grid[((r2 + 5) % 6) * 6 + c2];
  return grid[r1 * 6 + c2] + grid[r2 * 6 + c1];
}

function clean(raw) {
  return raw.toUpperCase().replace(/[^A-Z0-9]/g, '');
}

function makeEncDigrams(text) {
  let t = '';
  let i = 0;
  while (i < text.length) {
    const a = text[i];
    let b = text[i + 1];
    if (!b) { t += a; break; }
    if (a === b) { t += a + 'X'; i++; }
    else { t += a + b; i += 2; }
  }
  if (t.length % 2 !== 0) t += 'X';
  const pairs = [];
  for (let j = 0; j < t.length; j += 2) pairs.push(t[j] + t[j + 1]);
  return pairs;
}

function makeDecDigrams(text) {
  let t = text;
  if (t.length % 2 !== 0) t += 'X';
  const pairs = [];
  for (let i = 0; i < t.length; i += 2) pairs.push(t[i] + t[i + 1]);
  return pairs;
}

function updateEncrypt() {
  const kw = document.getElementById('enc-keyword').value;
  const txt = document.getElementById('enc-text').value;
  const grid = buildGrid(kw);
  renderGrid('enc-grid', grid, kw);

  const c = clean(txt);
  if (!c) {
    document.getElementById('enc-digrams').innerHTML = '<span class="placeholder-text">Enter text to see digrams…</span>';
    const r = document.getElementById('enc-result');
    r.textContent = 'Enter plaintext to encode';
    r.className = 'result-text empty';
    document.getElementById('enc-count').textContent = '';
    return;
  }

  const pairs = makeEncDigrams(c);
  const encPairs = pairs.map(p => encryptDigram(grid, p[0], p[1]));
  document.getElementById('enc-digrams').innerHTML = pairs.map(p => `<span class="digram-pill">${p}</span>`).join('');
  const result = encPairs.join('');
  const r = document.getElementById('enc-result');
  r.textContent = result;
  r.className = 'result-text';
  document.getElementById('enc-count').textContent = result.length + ' chars';
}

function updateDecrypt() {
  const kw = document.getElementById('dec-keyword').value;
  const txt = document.getElementById('dec-text').value;
  const grid = buildGrid(kw);
  renderGrid('dec-grid', grid, kw);

  const c = clean(txt);
  if (!c) {
    document.getElementById('dec-digrams').innerHTML = '<span class="placeholder-text">Enter ciphertext to see digrams…</span>';
    const r = document.getElementById('dec-result');
    r.textContent = 'Enter ciphertext to decode';
    r.className = 'result-text empty';
    document.getElementById('dec-count').textContent = '';
    return;
  }

  const pairs = makeDecDigrams(c);
  const decPairs = pairs.map(p => decryptDigram(grid, p[0], p[1]));
  document.getElementById('dec-digrams').innerHTML = pairs.map(p => `<span class="digram-pill">${p}</span>`).join('');
  const result = decPairs.join('');
  const r = document.getElementById('dec-result');
  r.textContent = result;
  r.className = 'result-text';
  document.getElementById('dec-count').textContent = result.length + ' chars';
}

function switchTab(tab) {
  document.querySelectorAll('.tab').forEach((t, i) => {
    const a = (tab === 'encrypt' && i === 0) || (tab === 'decrypt' && i === 1);
    t.classList.toggle('active', a);
    t.setAttribute('aria-selected', a);
  });
  document.getElementById('panel-encrypt').classList.toggle('active', tab === 'encrypt');
  document.getElementById('panel-decrypt').classList.toggle('active', tab === 'decrypt');
  document.querySelector('.scroll').scrollTop = 0;
}

function copyText(resultId, btnId) {
  const el = document.getElementById(resultId);
  if (!el || el.className.includes('empty')) return;
  const btn = document.getElementById(btnId);
  navigator.clipboard.writeText(el.textContent).then(() => {
    btn.classList.add('copied');
    btn.innerHTML = `<svg viewBox="0 0 24 24" style="width:13px;height:13px;stroke:currentColor;fill:none;stroke-width:2;stroke-linecap:round;stroke-linejoin:round"><polyline points="20 6 9 17 4 12"/></svg> Copied`;
    setTimeout(() => {
      btn.classList.remove('copied');
      btn.innerHTML = `<svg viewBox="0 0 24 24" style="width:13px;height:13px;stroke:currentColor;fill:none;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg> Copy`;
    }, 1800);
  }).catch(() => {});
}

// Init grids on load
renderGrid('enc-grid', buildGrid(''), '');
renderGrid('dec-grid', buildGrid(''), '');
</script>
</body>
</html>
